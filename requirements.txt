# SkyDiffRec / DiffusionREC-on-SkyFind environment
#
# Install CUDA-matched deep learning frameworks first if your server image
# does not already provide them:
#   torch
#   torchvision
#   detectron2
#
# MMCV must satisfy the copied mmdet code constraint:
#   1.2.4 <= mmcv-full <= 1.4.0

cython
numpy
scipy
Pillow
tqdm
matplotlib
opencv-python
six
terminaltables
timm
einops
transformers>=4.30,<5
blobfile
icecream
# DiffusionREC uses the CVPR'19 SceneGraphParser package that imports as
# `sng_parser`. Do not confuse it with the unrelated `sng-parser` package on PyPI.
SceneGraphParser==0.1.0
spacy>=3.6,<3.8
# `mmpycocotools` source builds are brittle on modern servers and are not
# required for the SkyFind baseline runtime path. We only need the standard
# `pycocotools` import surface used by copied mmdet modules.
pycocotools>=2.0.6
mmcv-full>=1.2.4,<=1.4.0
