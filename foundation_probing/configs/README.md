# Configs

This directory stores experiment configuration files for foundation probing.

Expected examples:

```text
subset manifests
prompt variant definitions
attention extraction settings
heatmap-to-box thresholds
crop expansion settings
```

Do not store model weights or generated attention tensors here.

